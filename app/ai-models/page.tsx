import { AIModelsContent } from "@/components/ai-models/AIModelsContent";

export const metadata = {
  title: "AI Models | Phoenix",
  description: "Explore the 5 AI models powering the Phoenix disaster rescue system",
};

export default function AIModelsPage() {
  return <AIModelsContent />;
}
